export const Location = (location) => {
    return `
        <section class="location card">
            <div class="locations__location">${locations.locations}</div>
            <div class="locations__landmark">${locations.landmark}</div>
           
        </section>
    `
}