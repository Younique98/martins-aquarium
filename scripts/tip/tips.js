export const Tip = (tip) => {
    return `
        <section class="tips card">
            <div class="tips__tip">${tips.tips}</div>
            <div class="tips__frequency">${tips.frequency}</div>
        </section>
    `
}